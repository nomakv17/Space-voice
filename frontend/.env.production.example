# =============================================================================
# SpaceVoice Frontend - Production Environment Variables
# =============================================================================
# For Vercel deployment, set these in the Vercel dashboard under Settings > Environment Variables

# API URL (your Railway backend URL)
NEXT_PUBLIC_API_URL=https://api.spacevoice.ai

# WebSocket URL (same as API URL, Railway handles WebSocket upgrades)
NEXT_PUBLIC_WS_URL=wss://api.spacevoice.ai
